PORT=3000
SESSION_SECRET=change-me-to-random-string
LOGIN_USERNAME=admin
LOGIN_PASSWORD=changeme
BASE_URL=http://localhost:3000
MAX_FILE_SIZE_MB=100
UPLOAD_DIR=uploads
DATA_DIR=data

# SMTP (без настройки письма логируются в консоль сервера)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=share@localhost
